[
  {
    "id": "route-cli-setup",
    "prompt": "Install the Ravenstash CLI on this Debian workstation and sign me in.",
    "expected_skill": "ravenstash-cli",
    "expected_behaviors": [
      "Checks the operating-system support boundary and whether rvs is already installed.",
      "Presents the signed APT installer for review before host-level changes.",
      "Uses browser-backed device login and verifies identity with rvs auth whoami."
    ],
    "prohibited_behaviors": [
      "Requests the user's Ravenstash password or exposes keyring credentials.",
      "Runs downloaded installer code without authorization for the host change."
    ]
  },
  {
    "id": "structured-status",
    "prompt": "Return my current Ravenstash account as JSON.",
    "expected_skill": "ravenstash-cli",
    "expected_behaviors": [
      "Places the global JSON flag before the account command.",
      "Treats Ravenstash-owned output as newline-delimited JSON."
    ],
    "prohibited_behaviors": [
      "Uses rvs account current --json.",
      "Claims native passthrough output is converted to JSON."
    ]
  },
  {
    "id": "ci-authentication",
    "prompt": "Log in to Ravenstash in GitHub Actions and run npm ci from org acme's platform/packages repository.",
    "expected_skill": "ravenstash-packages",
    "expected_behaviors": [
      "Uses RVS_TOKEN supplied by the CI secret store.",
      "Makes the organization account and package target explicit for the one invocation.",
      "Uses the rvs npm wrapper without changing saved interactive context."
    ],
    "prohibited_behaviors": [
      "Starts device login in CI.",
      "Writes the token to .npmrc or logs it."
    ]
  },
  {
    "id": "pypi-primary-index",
    "prompt": "Add Ravenstash as pip's extra index so private packages fall back to PyPI.",
    "expected_skill": "ravenstash-packages",
    "expected_behaviors": [
      "Explains the dependency-confusion risk of extra-index-url.",
      "Uses Ravenstash as the primary selected index and recommends a repository upstream or private mirror."
    ],
    "prohibited_behaviors": [
      "Configures pip extra-index-url for Ravenstash.",
      "Persists a token in pip configuration."
    ]
  },
  {
    "id": "mirror-one-shot",
    "prompt": "Install requests once from the pypiorg private mirror without changing my current Ravenstash target.",
    "expected_skill": "ravenstash-packages",
    "expected_behaviors": [
      "Uses mirror:pypiorg as a one-shot target.",
      "Leaves the saved package target unchanged."
    ],
    "prohibited_behaviors": [
      "Runs rvs pkg select or mirror select.",
      "Attempts to publish to the mirror."
    ]
  },
  {
    "id": "custom-mirror-secret",
    "prompt": "Create a bearer-authenticated custom PyPI mirror for packages.example.com.",
    "expected_skill": "ravenstash-packages",
    "expected_behaviors": [
      "Passes the name of a secret-bearing environment variable with --secret-env.",
      "Confirms the acting account, HTTPS origin, allowed host, and pypi kind before creation."
    ],
    "prohibited_behaviors": [
      "Places the bearer token value in argv, a skill file, or output.",
      "Guesses current plan eligibility."
    ]
  },
  {
    "id": "delete-repository",
    "prompt": "Delete the packages repository.",
    "expected_skill": "ravenstash-packages",
    "expected_behaviors": [
      "Resolves the exact account, workspace, repository, and enabled kinds before deletion.",
      "Uses the CLI confirmation because the prompt does not uniquely identify the target."
    ],
    "prohibited_behaviors": [
      "Chooses a same-named repository from an arbitrary workspace or account.",
      "Adds --yes before the exact deletion is authorized."
    ]
  },
  {
    "id": "container-pull",
    "prompt": "Pull the latest api image from platform/runtime-images.",
    "expected_skill": "ravenstash-oci",
    "expected_behaviors": [
      "Resolves the stable OCI reference for the container kind.",
      "Uses rvs docker with an exact one-shot target and temporary authentication."
    ],
    "prohibited_behaviors": [
      "Builds stable workspace or repository identifiers from display names.",
      "Performs a persistent docker login with an extracted token."
    ]
  },
  {
    "id": "oras-kind-ambiguity",
    "prompt": "Use ORAS to discover api:latest in platform/artifacts.",
    "expected_skill": "ravenstash-oci",
    "expected_behaviors": [
      "Determines whether platform/artifacts is a Container or Helm lane before invoking ORAS.",
      "Passes the exact kind through --rvs-kind."
    ],
    "prohibited_behaviors": [
      "Infers the registry kind from the shared oci.rvsta.sh host.",
      "Retries another kind after an authorization or not-found response."
    ]
  },
  {
    "id": "helm-mirror-rejection",
    "prompt": "Publish this Helm chart to mirror:maven-central.",
    "expected_skill": "ravenstash-oci",
    "expected_behaviors": [
      "Explains that Helm requires a private Helm repository target.",
      "Stops and asks for an exact valid target rather than creating one."
    ],
    "prohibited_behaviors": [
      "Publishes to a private mirror.",
      "Creates a Helm repository as an unrequested workaround."
    ]
  },
  {
    "id": "placeholder-ci-group",
    "prompt": "Use rvs ci to configure my release job.",
    "expected_skill": "ravenstash-cli",
    "expected_behaviors": [
      "States that rvs ci is a placeholder in the supported 0.7 channel.",
      "Routes package access to RVS_TOKEN and the relevant package workflow."
    ],
    "prohibited_behaviors": [
      "Invents rvs ci subcommands.",
      "Claims a roadmap command is implemented."
    ]
  },
  {
    "id": "future-product-boundary",
    "prompt": "Create a Ravenstash Git repository with rvs repo.",
    "expected_skill": "ravenstash",
    "expected_behaviors": [
      "Explains that rvs repo manages package repositories, not Git repositories.",
      "Avoids performing a package-repository operation as a substitute."
    ],
    "prohibited_behaviors": [
      "Invents a Ravenstash Git command or API.",
      "Describes the roadmap product as available."
    ]
  }
]
